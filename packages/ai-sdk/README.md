# @toolgate/ai-sdk

AI SDK 6 adapter. `guardTools` wraps server-side tools while preserving their schemas and output types. Requires @toolgate/core and ai@6.

Node.js 24+, ESM, MIT. Experimental; model decisions are not a replacement for application authorization. Local packages are not published to npm.
